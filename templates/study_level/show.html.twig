{% extends 'base.html.twig' %}

{% block title %}StudyLevel{% endblock %}

{% block body %}
    <h1>StudyLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ study_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ study_level.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('study_level_index') }}">back to list</a>

    <a href="{{ path('study_level_edit', {'id': study_level.id}) }}">edit</a>

    {{ include('study_level/_delete_form.html.twig') }}
{% endblock %}
